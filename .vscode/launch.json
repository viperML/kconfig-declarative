{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/build/kconfig-declarative",
            "args": [
                "${workspaceRoot}/manifest.json"
            ],
            "env": [],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "CMake: build"
        }
    ]
}
